
function getOrder(){try{return JSON.parse(localStorage.getItem(ORDER_KEY))}catch{return null}}
function ensureOrder(){let o=getOrder();if(!o&&new URLSearchParams(location.search).get('demo')==='1'){const c=[{id:1,qty:1}],t=totals(c);o={orderId:'ORD-DEMO2026',transactionId:'TXN-DEMO001',customer:{fullName:'عميل تجريبي'},items:c.map(i=>({...i,name:getProduct(i.id).name,price:getProduct(i.id).price})),totals:t,paymentMethod:'card',date:new Date().toLocaleString('ar-EG')};localStorage.setItem(ORDER_KEY,JSON.stringify(o))}return o}
document.addEventListener('DOMContentLoaded',()=>{const o=ensureOrder();if(!o){location.href='checkout.html';return}document.getElementById('payment-amount').innerHTML=`<span>قيمة الطلب</span><strong>${formatMoney(o.totals.total)}</strong>`;document.getElementById('cancel-payment').onclick=()=>{o.status='cancel';localStorage.setItem(ORDER_KEY,JSON.stringify(o));location.href='cancel.html'};document.getElementById('payment-form').addEventListener('submit',e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target).entries()),card=(d.card||'').replace(/\s/g,'');if(!/^\d{16}$/.test(card)||!/^\d{2}\/\d{2}$/.test(d.expiry||'')||!/^\d{3,4}$/.test(d.cvv||'')){showToast('تحقق من بيانات الاختبار.');return}document.getElementById('process-label').textContent='جارٍ تنفيذ المحاكاة...';document.getElementById('process-loader').style.display='inline-block';let result=d.result;if(result==='auto')result=card==='4000000000000002'?'failed':'success';setTimeout(()=>{o.transactionId='TXN-'+Math.random().toString(36).slice(2,11).toUpperCase();o.status=result;o.paymentLabel='بطاقة بنكية تجريبية';o.failureReason=result==='failed'?'رفض تجريبي من محاكي الدفع':'لا يوجد';localStorage.setItem(ORDER_KEY,JSON.stringify(o));location.href=result==='success'?'success.html':result==='failed'?'failed.html':'cancel.html'},1200)})});
